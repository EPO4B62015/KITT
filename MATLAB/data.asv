function status = data
%DATA Summary of this function goes here
%   Detailed explanation goes here
inc_data = EPOCommunications('transmit', 'S');
raw = strsplit(inc_data, {'D', 'U', 'A', 'udio ', '\n', ' '});
data_r = [raw(1,2), raw(1,3), raw(1,4), raw(1,5), raw(1,6), raw(1,7)];
data_rr = cellstr(data_r);
status = insertrows(, data_1, length(status));
end

